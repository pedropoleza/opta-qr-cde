# Spark Check-in — variáveis de ambiente (seção "Estado atual do código")
# Em produção (Vercel), o 👤 Time adiciona estes valores.

# Postgres (Supabase Sparkleads OS em produção; local para desenvolvimento)
DATABASE_URL="postgresql://postgres:SENHA@db.nsqwgjbgcdqyzozyaltz.supabase.co:5432/postgres"

# Segredos (32+ bytes aleatórios cada — ex.: openssl rand -hex 32)
JWT_SIGNING_KEY=""
TICKET_TOKEN_SECRET=""

# Domínio público do app (usado na URL dentro do QR Code)
APP_BASE_URL="http://localhost:3000"

# Etapa 4 — integração HighLevel (pendente de app no GHL Developer Portal)
GHL_CLIENT_ID=""
GHL_CLIENT_SECRET=""
GHL_REDIRECT_URI=""

# Etapa 3 — envio de e-mail próprio (se D1 incluir provedor próprio)
EMAIL_PROVIDER_KEY=""

# Seed inicial (npx prisma db seed)
SEED_ADMIN_EMAIL="info@sparkleads.pro"
SEED_ADMIN_PASSWORD=""
